This tool is only for fun, used to show images in IE.
Drag & Drop the folder to the dialog, and it will open IE automatically.
It will delete html file automatically when it exits.
NOTE: UI is ONLY CHINESE.
